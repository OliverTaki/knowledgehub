node_modules/
playwright-profile/
debug/
.cache/
.DS_Store
*.log

# Raw X Like data — never publish to public repo
data/raw/*.jsonl
data/raw/seen_x_status_ids.json

# Private generated outputs — rebuilt locally by npm run build:wire / build:site
data/processed/wire.json

content/wire/index.json
content/library/

# Public reviewed Wire JSON is deployable; raw/private source data remains ignored above.
public/library.json

# Wrangler and local deployment secrets
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
